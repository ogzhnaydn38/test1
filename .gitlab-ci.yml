stages:
  - deploy
  - build
  - test

build-and-deploy-server:
  stage: deploy
  image: docker:stable
  services:
    - docker:dind
  script:
    - cp $RSA_PRV_KEY  keys/rsa.key
    - sudo bash -c "docker build -f Dockerfile -t test1-service:latest ."
    - sudo bash -c 'cd undefined/tickatme/  && docker compose up -d test1-service'
  only:
    - main
# Notice: This file is generated by Mindbricks and formatted by prettier-plugin-yaml
